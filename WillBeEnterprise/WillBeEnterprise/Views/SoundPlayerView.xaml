<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:customView ="clr-namespace:WillBeEnterprise.Views.Custom"
              xmlns:extensions ="clr-namespace:WillBeEnterprise.Extensions"
             xmlns:behaviors="clr-namespace:WillBeEnterprise.Behaviors"
             x:Class="WillBeEnterprise.Views.SoundPlayerView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <customView:GradientButton Text="{extensions:Translate Play}" Style="{StaticResource GradientButtonStyle}">
                <Button.Behaviors>
                    <behaviors:EventToCommandBehavior EventName="Clicked" Command="{Binding PlayCommand}"/>
                </Button.Behaviors>
            </customView:GradientButton>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>